using Microsoft.Maui.Controls;

namespace ReperioNet.Sample.Maui;

/// <summary>Application root: a single demo page.</summary>
public class App : Application
{
    /// <summary>Initializes the application with the search demo page.</summary>
    public App()
    {
        MainPage = new MainPage();
    }
}
